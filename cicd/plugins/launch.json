{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "brightscript",
            "request": "launch",
            "name": "BrightScript Debug: Launch",
            "host": "${deviceIp}",
            "password": "${devicePassword}",
            "rootDir": "${workspaceFolder}/${projectPath}",
            "preLaunchTask": "build"
        }
    ]
}